services:
  mongo:
    image: mongo:7
    container_name: edubase-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: edubase
    volumes:
      - edubase_mongo_data:/data/db

volumes:
  edubase_mongo_data:
